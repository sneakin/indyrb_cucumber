<h1><%=h @project.name %></h1>
<h2>Stories</h2>
<%= link_to 'Add Story', new_project_story_path(@project) %>
<ul>
  <% @project.stories.each do |story| -%>
  <li>
    <%= link_to h(story.title), project_story_path(@project, story) %>
  </li>
  <% end -%>
</ul>